# oneinit 优化清单

> 基于 5,300+ 行 Rust 源码的全面审查，按优先级排列。

---

## P0 — 必须立即修复

### 1. 补充测试覆盖

**现状**：零测试。没有 `#[cfg(test)]`，没有 `tests/` 目录。

**风险**：会下载文件、修改 PATH、写注册表、操作 SQLite 的工具无测试，重构和迭代全靠手动验证。

**建议**：至少覆盖以下模块：

| 模块 | 测试重点 |
|------|----------|
| `core/manifest.rs` | SQLite CRUD、JSON 字段序列化/反序列化、边界值（空记录、重复插入） |
| `core/path_mgr.rs` | PATH 解析/修改/还原、去重逻辑、跨平台格式 |
| `core/capture/detector.rs` | `find_command` 命令查找、版本字符串解析、调度器注册 |
| `core/community_recipe.rs` | YAML 解析（合法/非法）、`verify` 校验各分支、模板变量替换 |
| `core/recipe.rs` | `resolve()` 匹配逻辑 |
| `core/sync.rs` | `oneinit.yaml` 解析、`envs_to_recipe_names()` 拼接 |

```bash
# 目标结构
src/core/manifest.rs    # 内嵌 #[cfg(test)] mod tests
src/core/path_mgr.rs    # 内嵌 #[cfg(test)] mod tests
src/core/capture/detector.rs  # 内嵌 #[cfg(test)] mod tests
src/core/community_recipe.rs # 内嵌 #[cfg(test)] mod tests
tests/                  # 集成测试（install -> list -> uninstall 完整流程）
```

### 2. 修复自定义探测器名称碰撞 bug

**现状**：`src/core/capture/detector.rs:138` 所有自定义探测器 `name()` 都返回 `"custom"`。

**影响**：多个自定义探测器并存时，`BTreeMap` 键互相覆盖，`scan()` 只保留最后一个。

**修复方案**：

```rust
// detector.rs — CustomDetector 结构体
impl EnvDetector for CustomDetector {
    fn name(&self) -> &str {
        &self.name  // 使用构造时传入的实际名称
    }
    // ...
}
```

---

## P1 — 高优先级改进

### 3. 补充内置配方

**现状**：`src/core/recipe.rs` 的 `resolve()` 仅一个 match arm（`python3.11`）。`frontend` preset 为空，`ai` preset 与 `python` 完全相同。

**建议**按以下顺序补充：

| 配方 | 理由 | 难度 |
|------|------|------|
| Node.js (LTS) | 前端/AI 开发必备，npm + npmmirror 配置 | 中（zip + post_install） |
| Git | 几乎所有开发者都需要 | 低（binary_copy） |
| Rust (stable) | rustup-init.exe，有官方 Windows 安装器 | 中（exe_silent） |
| Go | 官方 zip 发布，binary_copy 即可 | 低 |

### 4. 处理非 Windows 平台

**现状**：Python 配方在非 Windows 上 SHA256 为 `"NOT_AVAILABLE_FOR_NON_WINDOWS"` 字面量，安装必定失败。

**修复方案**：

```rust
// recipe.rs — resolve() 中为每个平台提供正确的 url + sha256
"python3.11" => {
    let (url, sha256) = if cfg!(target_os = "windows") {
        ("https://www.python.org/ftp/python/3.11.9/python-3.11.9-embed-amd64.zip",
         "009d6bf7...")
    } else if cfg!(target_os = "macos") {
        ("https://www.python.org/ftp/python/3.11.9/python-3.11.9-macos11.pkg",
         "实际sha256...")
    } else {
        ("https://www.python.org/ftp/python/3.11.9/Python-3.11.9.tgz.xz",
         "实际sha256...")
    };
    // ...
}
```

或者至少在非 Windows 平台上给明确错误而非 checksum 失败：

```rust
if sha256 == "NOT_AVAILABLE" {
    return Err(CoreError::Other("python3.11 is not yet supported on this platform".into()));
}
```

### 5. PATH 去重逻辑

**现状**：`src/core/path_mgr.rs:176-188` 向 Unix shell 配置文件追加 PATH 条目时不检查是否已存在，多次运行产生重复行。

**修复方案**：

```rust
fn append_to_shell_config(path: &Path, entry: &str) -> io::Result<()> {
    let content = fs::read_to_string(path)?;
    if content.contains(entry) {
        return Ok(()); // 已存在，跳过
    }
    let mut file = OpenOptions::new().append(true).open(path)?;
    writeln!(file, "\n# Added by oneinit\nexport PATH=\"{}:$PATH\"", entry)?;
    Ok(())
}
```

### 6. `msi_install` / `pkg_install` 实现

**现状**：`VALID_INSTALL_TYPES` 中声明了这两种类型，但 `install()` 的 match 语句未处理。

**影响**：用户在社区配方中使用这些类型会直接跳过或 panic。

**修复**：

```rust
"msi_install" => {
    let msi_path = download_dir.join(&platform_config.install_path);
    Command::new("msiexec").args(["/i", &msi_path.to_string_lossy(), "/qn"])
        .spawn()?.wait()?;
}
"pkg_install" => {
    let pkg_path = download_dir.join(&platform_config.install_path);
    Command::new("installer").args(["-pkg", &pkg_path.to_string_lossy(), "-target", "/"])
        .spawn()?.wait()?;
}
```

---

## P2 — 中优先级改进

### 7. 消除 `expect()` panic 点

**位置**：`src/core/mod.rs:17`

```rust
// 现状 — 直接 panic
dirs::home_dir().expect("cannot get user home directory")

// 建议 — 返回友好错误
pub fn home_dir() -> Result<PathBuf, CoreError> {
    dirs::home_dir().ok_or(CoreError::Other(
        "Cannot determine user home directory. Is $HOME set?".into()
    ))
}
```

### 8. 移除源码中的 emoji

**位置**：`src/core/recipe.rs:172` 和 `:228`

违反项目编码规范（ASCII-only），Windows 旧终端（cmd.exe、Git Bash 无字体支持）显示为乱码。

```
// 现状
"[OK] python3.11 installed successfully!"  (带 emoji)
// 应改为
"[OK] python3.11 installed successfully!"  (纯 ASCII)
```

### 9. manifest JSON 字段静默降级

**位置**：`src/core/manifest.rs` 多处 `unwrap_or_default()` / `unwrap_or(json!({}))`

**风险**：数据损坏时静默返回空值，用户无感知。

**建议**：对关键字段（path_entries、config_files）记录 warning 日志：

```rust
let path_entries: Vec<String> = match serde_json::from_str(&row) {
    Ok(v) => v,
    Err(e) => {
        eprintln!("[WARN] Failed to parse path_entries for {}: {}", name, e);
        vec![]
    }
};
```

### 10. TUI 展示社区配方

**现状**：`tui2/state.rs` 的 available 列表只调用 `recipe::list_recipes()`（内置），不加载社区配方。

**修复**：

```rust
fn load_available(&mut self) {
    // 内置配方
    self.available = recipe::list_recipes();
    // 社区配方
    if let Ok(community) = community_recipe::load_all() {
        for recipe in community {
            self.available.push(PackageInfo {
                name: recipe.name,
                version: recipe.version,
                installed: false, // 单独检查
                source: "community",
            });
        }
    }
}
```

### 11. packer 路径处理健壮化

**位置**：`src/core/migration/packer.rs:184-185`

```rust
// 现状 — 假设路径至少有 2 级祖先
let base = dest_path.ancestors().nth(2).unwrap_or(dest);
let relative = dest_path.strip_prefix(base).unwrap_or(dest_path);

// 建议 — 使用明确的基准目录
let base = archive_root; // tar.gz 解压根目录，由调用者明确指定
let relative = dest_path.strip_prefix(base)
    .map_err(|_| CoreError::Migration("path outside archive root".into()))?;
```

---

## P3 — 低优先级 / 清理

### 12. 添加 README.md

`Cargo.toml` 引用了 `readme = "README.md"` 但文件不存在，crates.io 发布会警告。

建议内容：快速开始、命令列表、安装方式（cargo install / 下载 binary）。

### 13. 清理 `tui2/` 命名

模块叫 `tui2/` 是因为 Windows 上原 `tui/` 目录有权限问题无法删除。应通过 git 操作修正：

```bash
git rm -r src/tui   # 即使本地删不掉，git 可跟踪删除
git mv src/tui2 src/tui
```

### 14. 清理 `temp/` 目录残留

17MB 的 `python-3.11.9-embed-amd64.zip` 在 repo 根目录，已 gitignore 但仍占磁盘空间。

---

## 完整问题索引

| # | 优先级 | 类型 | 位置 | 摘要 |
|---|--------|------|------|------|
| 1 | P0 | 测试 | 全项目 | 零测试覆盖 |
| 2 | P0 | Bug | `capture/detector.rs:138` | 自定义探测器名称碰撞 |
| 3 | P1 | 功能 | `recipe.rs` | 仅 1 个内置配方 |
| 4 | P1 | 兼容性 | `recipe.rs` | 非 Windows 平台直接失败 |
| 5 | P1 | Bug | `path_mgr.rs:176-188` | Unix PATH 重复追加 |
| 6 | P1 | 缺陷 | `community_recipe.rs` | msi/pkg 安装类型未实现 |
| 7 | P2 | 健壮性 | `core/mod.rs:17` | `expect()` panic |
| 8 | P2 | 规范 | `recipe.rs:172,228` | 源码含 emoji |
| 9 | P2 | 健壮性 | `manifest.rs` 多处 | JSON 静默降级 |
| 10 | P2 | 功能 | `tui2/state.rs` | TUI 不展示社区配方 |
| 11 | P2 | 健壮性 | `migration/packer.rs:184` | 路径祖先假设脆弱 |
| 12 | P3 | 清理 | 项目根 | 缺少 README.md |
| 13 | P3 | 清理 | `src/tui2/` | 模块命名遗留问题 |
| 14 | P3 | 清理 | `temp/` | 17MB 残留文件 |
