## OneInit - 项目完整策划案

## 写在前面重点！！
 1，开发此项目完全遵循一次一任务 一次一提提交的理念使用github cli 每次修改都提交
 2，任务化 新增plan.md每次要做什么先看看计划 要有规划 
 3， 新增Important.md每次遇到重要的东西例如新的命令 新的调用接口 代码结构写在里面
 4，开始开发 问问我要什么 完善开发需求严格遵守以上3点

## 一、项目定位

### 1.1 一句话定位

> **“拿到一台新电脑后，第一个要装的工具。装完它，这台电脑就是开发者就绪的机器。”**

### 1.2 名字的含义

`OneInit` = **One + Init**

- **One**：一条命令、一次操作、一键搞定
- **Init**：初始化、让系统进入就绪状态

用户第一次运行：

```bash
oneinit init
```

一个命令，整台电脑变成开发环境。

### 1.3 核心价值主张

**OneInit 不是包管理器，而是“环境初始化器”。**

它回答的问题不是“这个库装了吗”，而是 **“这台电脑现在能直接写代码了吗”** 。

| 维度 | 核心定义 |
| :--- | :--- |
| **管理单元** | 开发场景套装（“Python数据科学套装”、“React全栈套装”） |
| **安装逻辑** | 下载 + 解压 + 写入PATH + 换源 + 创建默认配置 + 启动服务 |
| **运行层级** | 完全用户态（`~/.oneinit/envs/`），**无需sudo，无需root** |
| **交互模式** | CLI为主（AI优先）+ GUI为辅（新手友好） |

### 1.4 目标受众与价值

| 受众 | 痛点 | OneInit 解决方案 |
| :--- | :--- | :--- |
| **AI Agent** | 现有CLI输出是给人看的文本，AI无法可靠解析 | 原生支持`--json`输出，AI可直接消费结构化结果 |
| **资深开发者** | 每台机器重复配置，碎片化工具多 | `oneinit sync` 一键还原整套团队环境 |
| **新手** | 不知道装什么、怎么配、配错了怎么办 | `oneinit search` 推荐套装，`oneinit init` 一步到位 |


## 二、与竞品的本质区别

| 对比维度 | `apt-get` | `npm` | `asdf` / `mise` | **OneInit** |
| :--- | :--- | :--- | :--- | :--- |
| **管理对象** | 系统库 | 语言依赖包 | 语言版本 | **开发场景套装** |
| **安装后状态** | 裸二进制 | 裸二进制 | 裸二进制 | **自动换源 + 写PATH + 调参** |
| **配置智能度** | 无 | 极少 | 无 | **全自动（镜像源+PATH+优化）** |
| **权限要求** | root | 普通用户 | 普通用户 | **完全用户态，零权限** |
| **AI友好度** | 极差 | 较差 | 一般 | **原生`--json`输出** |
| **卸载干净度** | 残留 | 残留 | 残留 | **按清单100%回滚** |
| **核心理念** | 安装软件包 | 管理项目依赖 | 切换语言版本 | **初始化整台电脑** |

## 四、核心功能

### 4 声明式配置文件

用户提交到Git仓库的 `oneinit.yaml`：

```yaml
# 团队共享的开发环境配置
envs:
  python: 3.7
  node: 18
  mysql: 8

mirrors:
  pip: tsinghua
  npm: taobao

post_install:
  - pip install -r requirements.txt
  - npm install
```

新成员 clone 代码后：

```bash
oneinit sync
```

整台机器瞬间和团队一致。

### 4.3 AI 模式（核心竞争力）

所有命令支持 `--json` 输出，AI Agent 直接消费：

```bash
oneinit install python3.7 --json
```

返回：

```json
{
  "status": "success",
  "action": "install",
  "package": "python3.7",
  "install_path": "/home/user/.oneinit/envs/python3.7",
  "env_vars_added": {"PYTHONPATH": "/home/.../lib"},
  "configs_applied": ["pip.conf → 清华源"],
  "manifest_id": "abc123",
  "duration_ms": 3420
}
```

AI 无需解析文本，直接读取 JSON 决定下一步。


## 五、技术栈

### 5.1 核心选型

| 层级 | 技术 | 理由 |
| :--- | :--- | :--- |
| **核心语言** | Rust | 单二进制分发、毫秒级启动、极致性能、无需运行时 |
| **CLI框架** | `clap` | 业界标准，支持子命令，轻松实现`--json`全局开关 |
| **异步运行时** | `tokio` | 大文件下载不卡顿 |
| **HTTP客户端** | `reqwest` | 断点续传、超时重试、校验和验证 |
| **解压/压缩** | `flate2` + `tar` + `zip` | 覆盖.tar.gz/.zip/.exe全格式 |
| **数据存储** | SQLite（`rusqlite`） | 安装清单、PATH备份、精准回滚 |
| **配置解析** | `serde_yaml` + `serde_json` | YAML给用户，JSON给机器 |
| **进度条** | `indicatif` | CLI下载进度展示 |
| **GUI（未来）** | Tauri | 复用Rust核心，打包<10MB |

### 5.2 架构图

```
┌─────────────────────────────────────────────────────────┐
│                      用户界面层                          │
│  ┌──────────────────┐    ┌──────────────────────────┐ │
│  │  CLI (clap)      │    │  GUI (Tauri，可选)       │ │
│  │  + --json 输出   │    │  调用 Core 库             │ │
│  └────────┬─────────┘    └─────────────┬────────────┘ │
├───────────┴────────────────────────────┴──────────────┤
│                核心引擎库 (Core - Rust)                 │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────┐ │
│  │ 解析器       │  │ 执行器      │  │ 回滚引擎       │ │
│  │ (serde_yaml) │→│ (tokio+     │→│ (rusqlite     │ │
│  │              │  │  reqwest)   │  │  + std::env)  │ │
│  └─────────────┘  └─────────────┘  └───────────────┘ │
├─────────────────────────────────────────────────────────┤
│               系统接口与持久化层                        │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────┐ │
│  │ OS API      │  │ SQLite      │  │ 文件系统       │ │
│  │ (PATH/注册表)│  │ (清单索引)   │  │ (~/.oneinit)  │ │
│  └─────────────┘  └─────────────┘  └───────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 5.3 关键实现要点

| 挑战 | 解决方案 |
| :--- | :--- |
| **跨平台PATH修改** | Linux/macOS写`~/.bashrc`；Windows调用`SetEnvironmentVariable`并广播`WM_SETTINGCHANGE` |
| **完整性校验** | 配方预存SHA256，下载后校验再解压 |
| **无需sudo** | 全部安装在`~/.oneinit/envs/`，只改用户级PATH |
| **镜像源智能选择** | 根据IP地理位置或延迟测试自动选最快源 |
| **环境变量回滚** | 安装前备份原始PATH到SQLite，卸载时精确恢复 |


## 六、开发路线图

### Phase 1: MVP（2-3个月）

**目标**：验证核心价值——"一键安装+自动配置"

| 任务 | 说明 |
| :--- | :--- |
| Rust CLI骨架 | clap + tokio 基础框架 |
| 核心引擎 | 下载 → 解压 → PATH写入 → 配置生成 |
| 首批包 | Python 3.7+（含自动换源），Node.js 18+（含淘宝镜像） |
| 清单系统 | SQLite存储安装记录 |
| 卸载功能 | 按清单完整回滚 |
| `--json`输出 | 所有命令支持AI模式 |
| 文档 | 中英文README + 快速入门 |

**验收标准**：`oneinit install python3.7` 后，`python --version` 直接可用，`pip install` 默认走清华源。

### Phase 2: 生态扩展（2-3个月）

**目标**：覆盖主流开发场景

| 任务 | 说明 |
| :--- | :--- |
| 包仓库扩展 | Java, Go, Rust, MySQL, Redis |
| `init`命令 | 预置套装（"AI开发套装"、"前端套装"） |
| `sync`命令 | 读取`oneinit.yaml`批量同步 |
| 社区配方机制 | GitHub PR接受新软件配方 |
| 配方数量 | 目标覆盖Top 50开发工具 |

### Phase 3: 体验优化（1-2个月）

**目标**：降低新手门槛 + 企业功能

| 任务 | 说明 |
| :--- | :--- |
| TUI界面 | 终端内交互式菜单（ratatui） |
| GUI桌面应用 | Tauri，复用Rust核心 |
| 企业功能 | 私有配方仓库、离线安装包 |
| 完善文档 | 视频教程、常见问题库 |


## 七、开源策略

| 维度 | 策略 |
| :--- | :--- |
| **许可证** | MIT 或 Apache 2.0 |
| **社区模式** | 核心代码 + 配方仓库分离，配方接受社区PR |
| **早期用户** | 瞄准AI开发者社区（LangChain、AutoGPT生态） |
| **增长飞轮** | 更多用户 → 更多配方贡献 → 覆盖更全 → 更多用户 |


## 八、总结

| 维度 | 结论 |
| :--- | :--- |
| **产品名** | **OneInit** —— 一条命令，初始化整台电脑 |
| **定位** | 开发者入职系统，用户态、零配置、AI可调用的环境初始化工具 |
| **市场** | 全球$75亿+市场，73%开发者受困于环境配置 |
| **差异化** | 不做包管理器，做“环境初始化器”；不跟apt/npm抢存量，切“新电脑第一装”的增量 |
| **技术** | Rust + SQLite，单二进制跨平台，毫秒级响应 |
| **路径** | CLI MVP → 生态扩展 → 体验优化 |


> **最终定位语：**
>
> *"OneInit 不是 apt，不是 npm，不是 mise。它是你拿到一台新电脑后第一个要装的东西——装完，这台电脑就是开发者就绪的机器。对 AI 来说，它是能听懂指令的手；对新人来说，它是告别配置地狱的门。"*

---
