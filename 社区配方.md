社区配方是OneInit生态的核心扩展机制——通过一个开放的配方仓库，让任何人都能为OneInit贡献新软件的安装配置（配方），实现“人人都能贡献，人人都能使用”。

**最简定义**：配方（Recipe）就是一个描述“如何安装并配置好某个软件”的JSON/YAML文件。它的设计借鉴了conda-forge的配方机制和Conan的本地配方索引仓库。
**重点**：安全性很重要 请重复阅读关于安全的部分 软件方面 当安装配方时（告诉用户会下载什么 写入什么 运行什么 ）必须醒目提醒
---

## 一、配方格式设计

### 1.1 配方文件结构

每个配方是一个YAML文件，采用**纯声明式**设计——只描述“要达成什么状态”，不写具体脚本。

```yaml
# recipes/python/3.7.yaml
name: python
version: "3.7.17"
description: "Python 3.7 with automatic mirror configuration"

# 平台支持
platforms:
  windows:
    url: "https://www.python.org/ftp/python/3.7.17/python-3.7.17.exe"
    sha256: "a8f8e5e..."
    install_type: "exe_silent"      # 静默安装
    install_args: ["/quiet", "/passive"]
    install_path: "Python37"
    path_add: ["{{install_dir}}", "{{install_dir}}/Scripts"]
    
  linux:
    url: "https://www.python.org/ftp/python/3.7.17/Python-3.7.17.tgz"
    sha256: "b8f8e5e..."
    install_type: "tar_extract"
    install_path: "python3.7"
    path_add: ["{{install_dir}}/bin"]
    
  darwin:
    url: "https://www.python.org/ftp/python/3.7.17/python-3.7.17-macosx10.9.pkg"
    sha256: "c8f8e5e..."
    install_type: "pkg_install"

# 后置配置（核心差异化能力）
post_install:
  # 环境变量
  env_vars:
    PYTHONPATH: "{{install_dir}}/lib"
    PYTHONUSERBASE: "{{user_home}}/.local"
  
  # 配置文件生成（自动换源）
  config_files:
    - path: "pip/pip.conf"
      template: |
        [global]
        index-url = {{mirror_pip}}
        trusted-host = {{mirror_pip_host}}
        timeout = 60
  
  # 执行命令
  commands:
    - "{{install_dir}}/bin/pip install --upgrade pip"
    - "{{install_dir}}/bin/pip install virtualenv"

# 依赖关系（确保先装什么）
depends:
  - "openssl"    # Python依赖OpenSSL
  - "zlib"

# 标签（用于搜索和分类）
tags:
  - "language"
  - "python"
  - "runtime"

# 维护者信息
maintainer:
  name: "John Doe"
  github: "johndoe"
  email: "john@example.com"
```

### 1.2 设计原则

| 原则 | 说明 |
| :--- | :--- |
| **纯声明式** | 不写Shell/Python脚本，只描述“最终状态”，OneInit引擎负责执行 |
| **平台感知** | 同一配方支持Windows/Linux/macOS三平台，各自独立配置 |
| **幂等性** | 多次执行结果一致（确保`install`可重复执行） |
| **模板变量** | 支持`{{install_dir}}`、`{{user_home}}`、`{{mirror_pip}}`等动态变量 |
| **无歧义** | 所有配置明确无二义性，参考“做菜指南”的无歧义原则 |

### 1.3 与竞品配方格式的对比

| 对比维度 | conda-forge | Homebrew | **OneInit** |
| :--- | :--- | :--- | :--- |
| **格式** | YAML + Jinja模板 | Ruby脚本 | **纯YAML，无脚本** |
| **复杂度** | 高（需理解构建系统） | 中（需懂Ruby） | **低（声明式，JSON即可）** |
| **配置自动优化** | 无 | 无 | **内置（镜像源、PATH、参数调优）** |
| **贡献门槛** | 高 | 中 | **低（复制模板改参数）** |

---

## 二、存储与分发

### 2.1 双仓库架构

```
┌─────────────────────────────────────────────────────────────┐
│                    官方配方仓库（GitHub）                    │
│  oneinit/recipes                                            │
│  ├── recipes/                                               │
│  │   ├── python/                                            │
│  │   │   ├── 3.7.yaml                                       │
│  │   │   ├── 3.8.yaml                                       │
│  │   │   └── 3.9.yaml                                       │
│  │   ├── nodejs/                                            │
│  │   ├── mysql/                                             │
│  │   └── ...                                                │
│  └── INDEX.json          # 全局索引（版本、依赖、标签）       │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ oneinit sync（定期拉取）
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    本地缓存（~/.oneinit/cache/）             │
│  ├── recipes/            # 缓存的配方文件                     │
│  └── INDEX.json          # 缓存的索引                        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 分发机制

| 层级 | 说明 |
| :--- | :--- |
| **官方仓库** | GitHub公开仓库，所有配方以PR方式合入 |
| **CDN分发** | 配方索引（INDEX.json）通过CDN加速，`oneinit search`秒级响应 |
| **本地缓存** | 首次使用后缓存到`~/.oneinit/cache/`，支持离线使用 |
| **私有仓库** | 企业可自建私有配方仓库（`oneinit remote add <url>`） |

### 2.3 索引设计（INDEX.json）

```json
{
  "version": 1,
  "last_updated": "2026-01-15T10:00:00Z",
  "packages": {
    "python": {
      "versions": ["3.7.17", "3.8.18", "3.9.18"],
      "latest": "3.9.18",
      "tags": ["language", "runtime"],
      "depends": ["openssl"],
      "maintainers": ["johndoe"]
    },
    "nodejs": {
      "versions": ["18.19.0", "20.10.0"],
      "latest": "20.10.0",
      "tags": ["language", "runtime"],
      "depends": [],
      "maintainers": ["janedoe"]
    }
  }
}
```

---

## 三、贡献流程

OneInit的配方贡献流程借鉴了conda-forge的staged-recipes机制和Astro的社区配方贡献流程。

### 3.1 完整流程图

```
┌─────────────┐
│  1. Fork    │  开发者Fork官方配方仓库
│  官方仓库   │
└──────┬──────┘
       ▼
┌─────────────┐
│  2. 创建    │  复制模板 → 填写配方 → 本地验证
│  配方文件   │  （oneinit verify recipe.yaml）
└──────┬──────┘
       ▼
┌─────────────┐
│  3. 提交    │  git commit → git push → 创建PR
│  PR        │
└──────┬──────┘
       ▼
┌─────────────┐
│  4. 审核    │  自动化检查（格式、SHA256、平台兼容性）
│  & 测试    │  + 人工审核（配方正确性、安全性）
└──────┬──────┘
       ▼
┌─────────────┐
│  5. 合并    │  合入main分支 → 更新INDEX.json → CDN同步
│  & 发布    │  → 所有用户可搜索安装
└─────────────┘
```

### 3.2 详细步骤

**步骤1：Fork仓库**

访问 `github.com/oneinit/recipes`，点击Fork。

**步骤2：创建配方文件**

```bash
# 复制模板
cp recipes/template.yaml recipes/<package>/<version>.yaml

# 编辑配方（填写url、sha256、post_install等）
vim recipes/python/3.7.yaml

# 本地验证
oneinit verify recipes/python/3.7.yaml
```

**步骤3：提交Pull Request**

```bash
git checkout -b add-python-3.7
git add recipes/python/3.7.yaml
git commit -m "feat: add Python 3.7.17 recipe"
git push origin add-python-3.7
# 在GitHub创建PR
```

**步骤4：自动化检查**

PR触发GitHub Actions自动执行：
- 格式校验（YAML语法、必填字段）
- SHA256校验（下载文件验证）
- 跨平台模拟安装（在Ubuntu/macOS/Windows容器中测试）
- 依赖检查（依赖的配方是否存在）

**步骤5：人工审核**

维护者审核：
- 软件来源是否可靠（官方源优先）
- 后置配置是否合理（镜像源选择、PATH设置）
- 是否有安全风险（是否包含恶意命令）

### 3.3 配方模板

```yaml
# recipes/template.yaml
# OneInit Recipe Template
# 复制此文件并修改以下字段

name: ""           # 必填：软件名称
version: ""        # 必填：版本号
description: ""    # 必填：简短描述

platforms:
  windows:
    url: ""        # 必填：下载地址
    sha256: ""     # 必填：SHA256校验值
    install_type: "exe_silent|zip_extract|msi_install"
    install_args: []
    install_path: ""
    path_add: []
  
  linux:
    url: ""
    sha256: ""
    install_type: "tar_extract|binary_copy"
    install_path: ""
    path_add: []
  
  darwin:
    url: ""
    sha256: ""
    install_type: "pkg_install|tar_extract"
    install_path: ""
    path_add: []

post_install:
  env_vars: {}     # 环境变量
  config_files: [] # 配置文件（模板）
  commands: []     # 后置命令

depends: []        # 依赖的软件
tags: []           # 标签
maintainer:        # 维护者信息
  name: ""
  github: ""
```

---

## 四、审核与质量控制

### 4.1 自动化检查

| 检查项 | 说明 |
| :--- | :--- |
| **YAML语法** | 确保配方文件格式正确 |
| **必填字段** | name/version/url/sha256必须存在 |
| **SHA256校验** | 下载文件计算SHA256，与配方一致 |
| **URL可访问** | 下载地址有效 |
| **平台覆盖** | 至少覆盖一个平台 |
| **依赖存在** | depends中的软件已在仓库中存在 |

### 4.2 人工审核标准

参考conda-forge的审核流程和Yocto的同行评审机制：

| 标准 | 要求 |
| :--- | :--- |
| **来源可信** | 优先官方源（python.org、nodejs.org），不接受第三方网盘 |
| **配置合理** | 镜像源选择、PATH设置符合最佳实践 |
| **无歧义** | 所有配置明确，没有模糊描述 |
| **安全性** | 不包含可疑命令、不请求过高权限 |
| **完整性** | 配方完整，能独立完成安装 |

### 4.3 维护者责任

配方合并后，贡献者自动成为该配方的维护者：
- 及时响应Issue（版本更新、安装失败报告）
- 定期更新版本（至少每年检查一次）
- 长期不维护的配方会被标记为“deprecated”

---

## 五、官方配方 vs 社区配方

| 维度 | 官方配方 | 社区配方 |
| :--- | :--- | :--- |
| **维护者** | OneInit核心团队 | 社区贡献者 |
| **质量要求** | 极高（完整测试+文档） | 较高（通过自动化+人工审核） |
| **更新频率** | 跟随软件LTS版本 | 由贡献者驱动 |
| **信任级别** | 高（默认信任） | 中（需用户确认或签名验证） |
| **标识** | 无特殊标识 | 显示 `[community]` 标签 |

---

## 六、激励与认可

| 激励方式 | 说明 |
| :--- | :--- |
| **贡献者名单** | README中列出所有配方贡献者 |
| **配方署名** | 配方文件中记录maintainer信息 |

---

## 七、安全与合规

| 风险 | 应对措施 |
| :--- | :--- |
| **恶意配方** | 人工审核 + 社区监督 + 用户举报机制 |
| **供应链攻击** | 所有下载强制SHA256校验，不接受未校验的URL |
| **权限滥用** | 配方只能操作用户目录（`~/.oneinit/`），无法获取root权限 |
| **版本伪造** | 配方合入前验证官方来源的版本真实性 |

---

**一句话总结**：OneInit的社区配方机制 = **GitHub PR驱动 + 声明式YAML格式 + 自动化校验 + 人工审核 + 双仓库分发**，让贡献者能用最简单的方式扩展生态，让用户能安全地使用海量配方。